module ECA
{

    imports {
	    Base,
	}
    
    item SwimmingBodyMASK {
        /* Use to mask the body part while swimming only. */


        /* DisplayName = HackForSwim, -- not show in invertory panel with no name item. */

        Type = Clothing,
        BodyLocation = SwimmingBodyMASK,
		ClothingItem = SwimmingBodyMASK,
        Weight = 0,
        WorldRender = FALSE,
        
        /* sample:
        DisplayCategory = MaleBody,
		Type = Clothing,
		ClothingItem = SwimmingBodyMASK,
		BodyLocation = ZedDmg,  -- that will cause texture problem.
		WorldRender = FALSE,
        */
    }

    model HiddenItem {
        mesh = hiddenItem,
        texture = transparent,
        scale = 1,
    }

    item SwimmingHackItem {
        Weight = 0,

        EquippedNoSprint = TRUE,
		RequiresEquippedBothHands = TRUE,
		RunSpeedModifier = 0.25,

        StaticModel = HiddenItem,
        WorldRender = FALSE,
        ReplaceInPrimaryHand = none swimming,
    }

}